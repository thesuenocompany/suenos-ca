(()=>{const list=document.getElementById('contest-list'),page=document.getElementById('contest-page');const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const money=v=>v?new Intl.NumberFormat('en-CA',{style:'currency',currency:'CAD',maximumFractionDigits:0}).format(v):'';const date=(v,tz='America/Vancouver')=>new Intl.DateTimeFormat('en-CA',{dateStyle:'long',timeStyle:'short',timeZone:tz}).format(new Date(v));const track=(name,c)=>{window.dataLayer=window.dataLayer||[];window.dataLayer.push({event:name,contest_id:c.id,contest_slug:c.slug,contest_type:c.contest_type||'standard',retailer_name:c.retailer_name||undefined,retailer_code:c.retailer_code||undefined,retail_parent_id:c.retail_parent_id||undefined})};async function get(url){const preview=new URLSearchParams(location.search).get('preview')==='1';const token=preview?sessionStorage.getItem('suenos-hotline-admin-token-v3')||'':'';const r=await fetch(url,{cache:'no-store',headers:token?{authorization:`Bearer ${token}`}:{}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||'Unable to load contest.');return d}function card(c){return `<article class="contest-card">${c.desktop_hero_url?`<img src="${esc(c.desktop_hero_url)}" alt="${esc(c.hero_alt||c.public_name)}">`:''}<div class="contest-card-body"><div class="kicker">${esc(c.state==='scheduled'?'COMING SOON':c.state==='closed'?'CLOSED':'ENTER NOW')}</div><h2>${esc(c.public_name)}</h2><p>${c.contest_type==='retail'&&c.retailer_name?`At ${esc(c.retailer_name)}. `:''}${esc(c.intro_copy||c.prize_title||'Paradise may be calling.')}</p><a class="orange-btn" href="/en-ca/contests/${esc(c.slug)}/">View Contest</a></div></article>`}if(list){get('/api/contests').then(d=>{list.innerHTML=(d.contests||[]).map(card).join('');document.getElementById('contest-empty').hidden=Boolean(d.contests?.length)}).catch(e=>list.innerHTML=`<p>${esc(e.message)}</p>`)}
if(page){const slug=new URLSearchParams(location.search).get('slug')||location.pathname.split('/').filter(Boolean).pop();get(`/api/contests?slug=${encodeURIComponent(slug)}${new URLSearchParams(location.search).get('preview')==='1'?'&preview=1':''}`).then(async ({contest:c})=>{if(new URLSearchParams(location.search).get('preview')==='1'){const b=document.createElement('div');b.className='contest-preview-banner';b.textContent='ADMIN PREVIEW · This contest may not be public yet.';document.body.prepend(b);}track(c.state==='closed'?'contest_closed_view':'contest_view',c);if(c.contest_type==='retail'&&window.renderRetailContest){document.title=`${c.public_name}${c.retailer_name?` at ${c.retailer_name}`:''} | Sueños Tequila`;await window.renderRetailContest(page,c,track);return;}if(c.contest_type==='photo_scrapbook'&&window.renderPhotoStoryContest){document.title=`${c.public_name} | Sueños Tequila`;await window.renderPhotoStoryContest(page,c,track);return;}if(c.layout_style==='sunfest_scrapbook'&&window.renderScrapbookContest){document.title=`${c.public_name} | Sueños Tequila`;await window.renderScrapbookContest(page,c,track);return;}document.title=`${c.public_name} | Sueños Tequila`;const active=c.state==='live',coming=c.state==='scheduled';const rules=c.external_rules_url?`<a href="${esc(c.external_rules_url)}" target="_blank" rel="noopener">Official Contest Rules</a>`:c.rules_pdf_url?`<a href="${esc(c.rules_pdf_url)}" target="_blank" rel="noopener">Official Contest Rules</a>`:`<details><summary>Official Contest Rules</summary><div class="contest-rules">${c.full_rules_html||''}</div></details>`;const custom=c.custom_question_enabled?`<div class="full"><label for="custom">${esc(c.custom_question_label||'Question')}</label>${c.custom_question_type==='short'?'<input id="custom" name="customResponse">':`<select id="custom" name="customResponse">${(c.custom_question_options||[]).map(x=>`<option>${esc(x)}</option>`).join('')}</select>`}</div>`:'';page.innerHTML=`<section class="contest-hero"><picture>${c.mobile_hero_url?`<source media="(max-width:800px)" srcset="${esc(c.mobile_hero_url)}">`:''}${c.desktop_hero_url?`<img src="${esc(c.desktop_hero_url)}" alt="${esc(c.hero_alt||c.public_name)}" style="object-position:${esc(c.hero_object_position)}">`:''}</picture><div class="contest-hero-copy"><div class="kicker">${esc(c.eyebrow||'SUEÑOS CONTEST')}</div><h1>${esc(c.public_name)}</h1><p>${esc(c.headline||c.intro_copy||'Paradise may be calling.')}</p><div class="contest-meta"><span>${esc(c.prize_title||'Prize')}</span>${c.prize_value?`<span>Approx. value ${money(c.prize_value)}</span>`:''}<span>Closes ${date(c.close_at,c.timezone)}</span></div>${active?'<a class="orange-btn" href="#contest-entry">Enter Now</a>':`<div class="contest-status">${coming?`Coming Soon. Opens ${date(c.start_at,c.timezone)}.`:'Contest Closed.'}</div>`}</div></section><section class="section"><div class="container contest-layout"><div><section class="contest-panel"><div class="kicker">THE PRIZE</div><h2>${esc(c.prize_title||c.public_name)}</h2><p>${esc(c.prize_description||'')}</p>${c.included_items?`<h3>Included</h3><p>${esc(c.included_items)}</p>`:''}${c.excluded_items?`<h3>Not included</h3><p>${esc(c.excluded_items)}</p>`:''}${c.redemption_restrictions?`<h3>Restrictions</h3><p>${esc(c.redemption_restrictions)}</p>`:''}</section><section class="contest-panel"><div class="kicker">HOW IT WORKS</div><div class="contest-steps"><div class="contest-step"><p>Complete the entry form.</p></div><div class="contest-step"><p>Submit before the closing date.</p></div><div class="contest-step"><p>A potential winner will be randomly selected.</p></div><div class="contest-step"><p>The selected entrant must answer a skill-testing question and satisfy the Official Rules.</p></div></div></section></div><aside class="contest-panel" id="contest-entry"><div class="kicker">${active?'ENTER NOW':coming?'COMING SOON':'CONTEST CLOSED'}</div><h2>${esc(c.public_name)}</h2>${active?`<form id="contest-form" class="contest-form"><div><label for="first">First name</label><input id="first" name="firstName" autocomplete="given-name" required></div><div><label for="last">Last name</label><input id="last" name="lastName" autocomplete="family-name" required></div><div class="full"><label for="email">Email address</label><input id="email" name="email" type="email" autocomplete="email" required></div>${c.phone_enabled?`<div class="full"><label for="phone">Phone number</label><input id="phone" name="phone" autocomplete="tel" ${c.phone_required?'required':''}></div>`:''}<div><label for="city">City</label><input id="city" name="city" autocomplete="address-level2" ${c.city_required?'required':''}></div><div><label for="postal">Postal code</label><input id="postal" name="postalCode" autocomplete="postal-code" ${c.postal_required?'required':''}></div><div class="full"><label for="province">Province</label><select id="province" name="province" ${c.province_required?'required':''}><option value="">Select</option>${['BC','AB','SK','MB','ON','QC','NB','NS','PE','NL','YT','NT','NU'].map(x=>`<option value="${x}">${x}</option>`).join('')}</select></div>${custom}<div class="full"><label class="contest-check"><input type="checkbox" name="ageConfirmed" required><span>I confirm that I am 19 years of age or older.</span></label></div><div class="full"><label class="contest-check"><input type="checkbox" name="rulesConfirmed" required><span>I have read and agree to the Official Contest Rules.</span></label></div>${c.marketing_enabled?`<div class="full"><label class="contest-check"><input type="checkbox" name="marketingConsent"><span>${esc(c.marketing_consent_text)}</span></label><p class="contest-small">You do not need to subscribe to enter this contest.</p></div>`:''}<div class="contest-honeypot"><label>Website<input name="website" tabindex="-1" autocomplete="off"></label></div><input type="hidden" name="formStartedAt" value="${Date.now()}"><div class="full"><div class="cf-turnstile" data-sitekey="0x4AAAAAAD9MP21ca7C8BGsS" data-action="contest"></div></div><div class="full contest-rules">${esc(c.abbreviated_rules||'No purchase necessary. Odds depend on the number of eligible entries. A skill-testing question is required.')} ${rules}</div><button class="orange-btn full" type="submit">Submit Entry</button><p id="contest-form-status" class="full" role="status" aria-live="polite"></p></form>`:`<div class="contest-status">${coming?`Entries open ${date(c.start_at,c.timezone)}.`:`Entries closed ${date(c.close_at,c.timezone)}.`}</div>${c.publish_winner&&c.published_winner_name?`<p>Winner: ${esc(c.published_winner_name)}${c.published_winner_city?`, ${esc(c.published_winner_city)}`:''}</p>`:''}`}</aside></div></section>`;const form=document.getElementById('contest-form');if(form){let started=false;form.addEventListener('focusin',()=>{if(!started){started=true;track('contest_entry_start',c)}});form.addEventListener('submit',async e=>{e.preventDefault();const status=document.getElementById('contest-form-status'),button=form.querySelector('button[type=submit]');button.disabled=true;status.textContent='Submitting…';track('contest_entry_submit',c);const fd=new FormData(form),params=new URLSearchParams(location.search);const payload=Object.fromEntries(fd.entries());payload.slug=c.slug;payload.ageConfirmed=fd.get('ageConfirmed')==='on';payload.rulesConfirmed=fd.get('rulesConfirmed')==='on';payload.marketingConsent=fd.get('marketingConsent')==='on';payload.turnstileToken=fd.get('cf-turnstile-response');payload.utmSource=params.get('utm_source');payload.utmMedium=params.get('utm_medium');payload.utmCampaign=params.get('utm_campaign');payload.utmContent=params.get('utm_content');payload.referrer=document.referrer;try{const r=await fetch('/api/contests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}),d=await r.json();if(!r.ok)throw Object.assign(new Error(d.message),{code:d.code});track('contest_entry_success',c);form.outerHTML=`<div class="contest-success"><h2>${esc(d.confirmation?.heading||'You’re in. Paradise may be calling.')}</h2><p>${esc(d.confirmation?.message||'Your entry has been received.')}</p>${d.confirmation?.ctaUrl?`<a class="orange-btn" href="${esc(d.confirmation.ctaUrl)}">${esc(d.confirmation.ctaLabel||'Keep Exploring')}</a>`:''}</div>`}catch(err){if(err.code==='duplicate')track('contest_entry_duplicate',c);status.textContent=err.message||'Please check the required fields and try again.';button.disabled=false}})}}).catch(e=>page.innerHTML=`<section class="section"><div class="small"><h1>Contest unavailable</h1><p>${esc(e.message)}</p></div></section>`)} })();