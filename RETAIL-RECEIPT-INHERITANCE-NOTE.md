# Retail receipt bonus inheritance update

- Retail master controls receipt bonus settings by default.
- Retailer sub-contests inherit the master receipt bonus setting and configuration.
- Each retailer can override receipt bonus ON or OFF from its editor.
- Public retail contest renderer is cache-busted as `retail-contest-20260807-receipts.js`.
- Existing Account Cooler Giveaway master was enabled for receipt bonus; existing retailer children inherit it.
