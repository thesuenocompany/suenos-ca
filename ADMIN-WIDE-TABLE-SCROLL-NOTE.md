# Admin wide table scroll fix

Wide Admin tables now stay inside their card/workspace and receive a dedicated horizontal scrollbar. The Contest Entries table uses a wider natural table width so receipt, address and action information is no longer crushed into narrow columns.
